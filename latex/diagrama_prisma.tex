% Diagrama de fluxo PRISMA 2020

\begin{tikzpicture}[
	node distance=8mm and 12mm,
	% Caixa estreita — fluxo central (textos curtos)
	flowbox/.style={draw, rectangle, align=center, text width=4.5cm, minimum height=3em, font=\footnotesize, outer sep=0pt},
	% Caixa larga — identificação e exclusão (listas)
	listbox/.style={draw, rectangle, align=left, text width=5.5cm, minimum height=3em, font=\footnotesize, outer sep=0pt},
	% Label lateral
	phase/.style={draw, rectangle, rounded corners, fill=cyan!30, font=\footnotesize\bfseries, inner sep=2ex, outer sep=0pt, minimum height=4em},
	% Seta
	arrow/.style={thick, ->, >=stealth}
]

% --- Identificação ---
\node[listbox, text width=5.5cm] (n1) {
	Registros identificados nas bases de dados (n~=~137):
	\begin{itemize}[leftmargin=1.5em, nosep, topsep=2pt]
		\item Scopus (n~=~16)
		\item SciELO (n~=~5)
		\item Portal de Periódicos da CAPES (n~=~30)
		\item EconPapers/RePEc (n~=~24)
		\item ANPEC (n~=~62)
	\end{itemize}
};

% --- Inclusão manual (à direita da Identificação) ---
\node[flowbox, text width=3.8cm, right=15mm of n1] (nmanual) {Inclusão manual\\(n~=~12)};

% --- Triagem ---
\node[flowbox, below=12mm of n1] (n2) {Registros após remoção\\de duplicatas (n~=~130)};
\draw[arrow] (n1) -- (n2);
\draw[arrow] (nmanual) |- (n2);

\node[flowbox, below=of n2] (n3) {Registros triados (n~=~130)};
\draw[arrow] (n2) -- (n3);

% --- Elegibilidade ---
\node[flowbox, below=12mm of n3] (n4) {Publicações avaliadas\\em texto completo (n~=~130)};
\draw[arrow] (n3) -- (n4);

\node[listbox, text width=4.5cm, right=15mm of n4] (n4r) {
	Publicações excluídas (n~=~86):
	\begin{itemize}[leftmargin=1.5em, nosep, topsep=2pt]
		\item Não é estudo científico (n~=~10)
		\item Não avalia instrumentos da PNDR (n~=~39)
		\item Não aplica método econométrico (n~=~24)
		\item Versão \textit{working paper} de artigo publicado (n~=~8)
		\item Documento anterior a 2005 (n~=~2)
		\item Variáveis de resultado fora do escopo (n~=~3)
	\end{itemize}
};
\draw[arrow] (n4) -- (n4r);

% --- Inclusão ---
\node[flowbox, below=20mm of n4] (n5) {Estudos incluídos na revisão\\(n~=~44)};
\draw[arrow] (n4) -- (n5);

% --- Labels laterais (alinhados na mesma vertical) ---
\path (n1.west) ++(-15mm,0) coordinate (labelx);
\node[phase] at (labelx |- n1) {\rotatebox{90}{\textbf{Identificação}}};
\node[phase] at (labelx |- {$(n2)!0.5!(n3)$}) {\rotatebox{90}{\textbf{Triagem}}};
\node[phase] at (labelx |- n4) {\rotatebox{90}{\textbf{Elegibilidade}}};
\node[phase] at (labelx |- n5) {\rotatebox{90}{\textbf{Inclusão}}};

\end{tikzpicture}
